My dotfiles. Designed for apt based Linux distributions but will also run on Mac if the necessary dependencies are already installed (see the prompt when you run the script).

Currently assumes you have `git` already installed.

```
curl https://raw.githubusercontent.com/jaysh/dotfiles/master/bootstrap.sh | bash
```
